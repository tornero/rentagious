source 'http://rubygems.org'

ruby '2.3.3'

# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '4.1.8'
# Use sqlite3 as the database for Active Record
#gem 'sqlite3'
gem 'pg', '~> 0.21.0'

# Use Uglifier as compressor for JavaScript assets
gem 'uglifier', '>= 1.3.0'
# Use CoffeeScript for .js.coffee assets and views
gem 'coffee-rails', '~> 4.0.0'
# See https://github.com/sstephenson/execjs#readme for more supported runtimes
# gem 'therubyracer',  platforms: :ruby

# Use jquery as the JavaScript library
gem 'jquery-rails'
# Turbolinks makes following links in your web application faster. Read more: https://github.com/rails/turbolinks
gem 'turbolinks'

# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
gem 'jbuilder', '~> 2.0'
# bundle exec rake doc:rails generates the API under doc/api.
gem 'sdoc', '~> 0.4.0',          group: :doc

# From Bootstrap SASS instructions on GitHub
gem 'bootstrap-sass', '~> 3.3.6' # Use Bootstrap library
gem 'sass-rails', '>= 3.2' # Use SCSS for stylesheets

gem 'devise' # for authentication

gem 'toastr-rails','>= 0' # for notifications

gem 'sendgrid-rails', '~> 2.0', '>= 2.0.5' # for sending emails

gem 'omniauth'
gem 'omniauth-facebook' # for authentication

gem 'paperclip', '~> 3.5.3' # for file upload mgmt 
gem 'aws-sdk', '< 2.0' # for aws cloud storage of images/files

gem 'certified'

gem 'geocoder'

gem 'momentjs-rails', '>= 2.9.0'
gem 'bootstrap3-datetimepicker-rails', '~> 4.17.47'

gem 'rails_12factor', group: :production # for heroku features like better error messages

gem 'private_pub' #gems for realtime messaging
gem 'thin' #gems for realtime messaging

gem 'local_time' #for getting user's local time

gem 'ransack' #for searching functionality

gem 'stripe' #for cc payment processing

#group :development, :test do
#  gem 'dotenv-rails'
#end

# Use ActiveModel has_secure_password
# gem 'bcrypt', '~> 3.1.7'

# Use unicorn as the app server
# gem 'unicorn'

# Use Capistrano for deployment
# gem 'capistrano-rails', group: :development

# Use debugger
# gem 'debugger', group: [:development, :test]

# Windows does not include zoneinfo files, so bundle the tzinfo-data gem
gem 'tzinfo-data', platforms: [:mingw, :mswin]
