<div class="panel">
    <div class="panel-body">
        <%= image_tag avatar_url(message.user), class: "img-circle avatar-small" %>
        <strong><%= message.user.fullname %></strong>

        <span class="pull-right"><%= local_time_ago(message.created_at) %></span>
        <br>
        <div class="row-space-2">
            <%= message.content %>
        </div>
    </div>
</div>